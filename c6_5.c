int c6_5()
{
	//输出一个有一定格式要求的下三角矩阵
	int NUM = 10;

	int a[10][10]; //变量定义时不可以用变量去定义,要用常量(包括宏定义)

	int i,j;

	int (*ptr2)[10];
	
	ptr2 = a;

	for(i = 0; i<10;i++)
		for(j = 0; j<10; j++)
		{
			*(*(ptr2+i)+j) = 0; //一定确认好定义的指针是指向一维数组还是指向整数的,指向整数可以用*(ptr1+i*NUM+j),指向
		}

	for(i = 0; i<10;i++)
		for(j=0;j<10;j++)
		{
			if(i>=j)
			{
				*(*(ptr2+i)+j) = j + 1;
			}
		}
	for(i = 0; i<10;i++)
	{
		for(j = 0; j<10; j++)
		{
			printf("%3d",*(*(ptr2+i)+j));
		}
	printf("\n");
	}
}